// package.json'a bu scriptleri ekleyin:
{
  "scripts": {
    "build": "CI=false npm run build",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build"
  }
}
{
  "name": "bizmind-ai",
  "version": "1.0.0",
  "description": "KOBİ'ler için AI destekli satış analiz asistanı",
  "keywords": ["react", "typescript", "ai", "sales", "crm", "analysis"],
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-scripts": "5.0.1",
    "typescript": "^4.9.5",
    "tailwindcss": "^3.3.0",
    "lucide-react": "^0.263.1",
    "papaparse": "^5.4.1",
    "@types/papaparse": "^5.3.14",
    "html2canvas": "^1.4.1",
    "jspdf": "^2.5.1"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "deploy": "npm run build && vercel --prod"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "@types/react": "^18.0.28",
    "@types/react-dom": "^18.0.11"
  }
}
